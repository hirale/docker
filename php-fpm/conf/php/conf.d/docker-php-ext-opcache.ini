; opcache is built into PHP core on 8.4/8.5 (no opcache.so module to load).
; Dev override: keep opcache disabled so code edits take effect immediately
; (the mounted php.ini-production sets opcache.enable=1 + validate_timestamps=0).
opcache.enable=0
